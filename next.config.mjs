/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true, 
  },
  basePath: '/Taran-Wadhawan-Portfolio', // Replace 'portfolio' with your exact GitHub repository name
};

export default nextConfig;